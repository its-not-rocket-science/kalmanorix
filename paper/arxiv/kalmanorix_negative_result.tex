\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{siunitx}
\usepackage{url}
\usepackage{tcolorbox}
\usepackage[hidelinks]{hyperref}

\title{Negative Result Preprint:\\
No Reliable Kalman-over-Mean Gain in Domain-Balanced Retrieval}
\author{Kalmanorix Project Team}
\date{May 2026}

\begin{document}
\maketitle

\begin{abstract}
We report a concise, reproducible negative-result preprint for mixed-domain retrieval fusion. In the canonical domain-balanced C100 confirmatory run ($n_{\text{pairs}}=1193$), Kalman-minus-Mean $\Delta$ nDCG@10 is $-9.258801070226193\times10^{-6}$ with Holm-adjusted $p=1.0$; recall@100 delta is 0.0. Compute overhead is bounded (latency ratio 1.0722551296204925; FLOPs ratio 1.0). The benchmark verdict is \texttt{inconclusive\_sufficiently\_powered}. Relative to the TMLR manuscript, this version prioritizes the negative empirical result and exact reproduction instructions.
\end{abstract}

\begin{tcolorbox}[colback=gray!8,colframe=black,title=Scope of conclusion]
This paper makes a \textbf{single bounded claim}: under the tested protocol (domain-balanced mixed-domain benchmark, \texttt{max\_candidates=100}, deterministic embedding regime), Kalman-style uncertainty fusion did \emph{not} demonstrate a reliable practical improvement over mean fusion.

It does \textbf{not} claim that Kalman methods are universally ineffective, nor that mean fusion is always best.
\end{tcolorbox}

\section{Setup (confirmatory track only)}
Benchmark: \texttt{mixed\_beir\_v1.2.0} with train=5078, validation=634, test=1193, minimum per-domain test count 126. Confirmatory settings: \texttt{domain-balanced=true}, \texttt{max\_candidates=100}, paired query-level analysis, Holm correction, and practical-effect gate.

Primary endpoint: paired $\Delta$ nDCG@10 (Kalman minus Mean). Decision gates match the registered artifacts: practical threshold $|\Delta|\ge 0.02$, Holm-adjusted $p<0.05$, latency ratio $\le 1.5$, and FLOPs ratio $\le 1.1$.

\section{Main negative result}
The final confirmatory outputs (from \texttt{results/evidence\_registry.json} and \texttt{results/claim\_gate.json}) are:
\begin{itemize}
  \item $\Delta$ nDCG@10 $=-9.258801070226193\times10^{-6}$
  \item Holm-adjusted $p=1.0$
  \item $\Delta$ recall@100 $=0.0$
  \item Latency ratio (Kalman/Mean) $=1.0722551296204925$
  \item FLOPs ratio (Kalman/Mean) $=1.0$
  \item Verdict: \texttt{inconclusive\_sufficiently\_powered}
\end{itemize}

Interpretation: the run had enough power for the predeclared practical effect scale, but no practically meaningful Kalman-over-Mean gain was detected.

\section{Baseline matrix}
\begin{table}[ht]
\centering
\caption{Baseline matrix from \texttt{canonical\_benchmark\_v3\_fast\_1193\_c100\_balanced} ($n=1193$ paired test queries, domain-balanced, \texttt{max\_candidates}=100).}
\label{tab:baseline-matrix}
\input{tables/main_results}
\end{table}

\section{What this result does and does not mean}
\textbf{What it supports.}
\begin{itemize}
  \item In this protocol, Kalman fusion did not clear the practical-significance and multiplicity-adjusted evidence gate.
  \item Mean fusion remains a strong low-complexity reference under the tested configuration.
\end{itemize}

\textbf{What it does not support.}
\begin{itemize}
  \item It does not establish universal inferiority of uncertainty-aware fusion.
  \item It does not rule out gains under different embeddings, domains, candidate budgets, or uncertainty estimators.
\end{itemize}

\section{Reproducibility: exact commands and artifact paths}
\textbf{Evidence source-of-truth artifacts}
\begin{itemize}
  \item \texttt{results/evidence\_registry.json}
  \item \texttt{results/claim\_gate.json}
  \item \texttt{results/canonical\_benchmark\_v3\_fast\_1193\_c100\_balanced/summary.json}
  \item \texttt{results/negative\_result\_diagnostics/report.md}
  \item \texttt{results/negative\_result\_diagnostics/summary.json}
\end{itemize}

\textbf{Commands}
\begin{verbatim}
# from repository root
PYTHONPATH=src python scripts/build_evidence_registry.py
python scripts/build_claim_gate.py

# build this arXiv preprint
cd paper/arxiv
pdflatex kalmanorix_negative_result.tex
bibtex kalmanorix_negative_result
pdflatex kalmanorix_negative_result.tex
pdflatex kalmanorix_negative_result.tex

# repository-required checks
cd /workspace/kalmanorix
ruff format --check .
pytest -m "not integration and not stress"
\end{verbatim}

\section{Recommended citation and artifact use}
When citing this preprint, cite it as a \textbf{negative-result, protocol-local empirical study} rather than a general method-validation paper. For numeric claims, prefer artifact-backed references to:
\texttt{results/evidence\_registry.json} and \texttt{results/claim\_gate.json}. For system-level context, include \texttt{results/canonical\_benchmark\_v3\_fast\_1193\_c100\_balanced/summary.json} and \texttt{results/negative\_result\_diagnostics/summary.json}. Avoid extending conclusions beyond the tested protocol unless new confirmatory evidence is provided.

\bibliographystyle{plain}
\bibliography{references}

\end{document}
